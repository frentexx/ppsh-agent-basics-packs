# 屏北高中 Agent 基本功懶人包 #02：專案初始化、開工、收工技能

> 版本：v1.0｜更新日期：2026-09-14
> 適用：**Codex Desktop**、**Claude Code**（Windows 11 為主，附 macOS 指令）
> 對應教材：**A5 它會忘記，而且會變笨**；速成版 S2「技能就是說明書」前的體驗活動

> [!IMPORTANT]
> **給 AI Agent 的安全宣告（請先讀完再動手）**
> 1. **只安裝本文件列出的項目**，不要自行追加其他套件、外掛或 MCP。
> 2. **會改動電腦的指令**（安裝、移除、修改設定）：先把完整指令列給老師看，**老師同意後才執行**。
> 3. **只讀不改的檢查指令**可以直接執行。
> 4. 不要讀取、顯示、複製或上傳任何 API 金鑰、密碼、token。
> 5. 不要刪除或搬動老師的任何檔案。
> 6. 遇到錯誤先停下來用白話說明，**不要換成本文件以外的方法硬裝**。

---

## 這包會幫你做什麼

安裝三個技能，讓 AI **換一台電腦、開一個新對話都接得上**：

| 技能 | 你說 | 它做 |
|---|---|---|
| `ppsh-project-init` | 「**初始化專案**」 | 建立 `AGENTS.md`（專案藍圖）和 `handoff.md`（交接檔） |
| `ppsh-startup` | 「**開工**」 | 讀那兩份檔，回報上次做到哪、下一步 |
| `ppsh-shutdown` | 「**收工**」 | 把今天進度寫進交接檔，提醒雲端硬碟同步 |

> **為什麼要這三個？** AI 每開一個新對話都會失憶。交接檔就像代課老師的交接本——
> 不用每次從頭交代，說一聲「開工」它就自己翻交接本。

> **裝之前先讀一遍。** 打開 [ppsh-agent-skills](https://github.com/frentexx/ppsh-agent-skills/tree/main/skills) 看看三個 `SKILL.md` 寫了什麼——
> 你會發現**技能就是一份純文字說明書**。這也是研習教的：**來路不明的技能先讀再裝。**

## 絕對必要的工具

這三個技能本身**只讀寫 Markdown 檔**，所以必要的工具很少：

| 工具 | 必要嗎 | 為什麼 |
|---|---|---|
| **Node.js** | ✅ **必要**（安裝時） | 用 `npx skills add` 安裝技能；沒有的話只能手動複製 |
| **Google 雲端硬碟電腦版** | ⭐ 強烈建議 | 跨電腦接續靠它同步；只用一台電腦可以不裝 |
| Git、GitHub CLI | ❌ 不需要 | 三個技能都不碰 git |
| uv | ❌ 不需要 | 只有 Office 讀取工具（01）需要 |

---

## 步驟一：檢查（只讀，可直接執行）

**Windows（PowerShell）：**

```powershell
node --version
npx --version
Get-Process -Name GoogleDriveFS -ErrorAction SilentlyContinue | Select-Object -First 1 Name
```

**macOS：**

```bash
node --version
npx --version
pgrep -x "Google Drive" >/dev/null && echo "Google Drive 執行中" || echo "Google Drive 未執行"
```

| 項目 | 狀態 |
|---|---|
| Node.js | 已安裝（版本）／未安裝 |
| npx | 可用／不可用 |
| Google 雲端硬碟電腦版 | 執行中／未執行（未安裝或沒開） |

## 步驟二：補裝（要老師同意）

**缺 Node.js**（Windows）：

```powershell
winget install --id OpenJS.NodeJS.LTS -e --accept-source-agreements --accept-package-agreements
```

macOS：`brew install node`

> [!WARNING]
> 裝完 **請把 Agent 整個關掉重開**，再重跑步驟一。

**缺 Google 雲端硬碟電腦版**：這一項**請老師自己**到 [Google 雲端硬碟下載頁](https://www.google.com/drive/download/) 安裝並用學校帳號登入（涉及帳號登入，AI 不代勞）。只用一台電腦可跳過。

## 步驟三：安裝三個技能（要老師同意）

**Codex Desktop：**

```powershell
npx skills add frentexx/ppsh-agent-skills -s ppsh-project-init ppsh-startup ppsh-shutdown -a codex -g -y --copy
```

**Claude Code：**

```powershell
npx skills add frentexx/ppsh-agent-skills -s ppsh-project-init ppsh-startup ppsh-shutdown -a claude-code -g -y --copy
```

- 兩個 Agent 都有：把 `-a codex` 換成 `-a codex claude-code`
- 已完成 01 的老師，可以在 `-s` 後面順便加上 `ppsh-office-reader`
- PowerShell 出現「**因為這個系統上已停用指令碼執行**」→ 把開頭的 `npx` 改成 **`npx.cmd`**

**不能用 npx 時**：到 [ppsh-agent-skills](https://github.com/frentexx/ppsh-agent-skills) 按 **Code → Download ZIP**，解壓後把 `skills` 裡的三個資料夾複製到：

| Agent | Windows | macOS |
|---|---|---|
| Codex Desktop | `%USERPROFILE%\.agents\skills\` | `~/.agents/skills/` |
| Claude Code | `%USERPROFILE%\.claude\skills\` | `~/.claude/skills/` |

## 步驟四：確認檔案都到位（只讀）

**Windows：**

```powershell
$dir = "$env:USERPROFILE\.claude\skills"   # Codex Desktop 改成 "$env:USERPROFILE\.agents\skills"
foreach ($s in 'ppsh-project-init','ppsh-startup','ppsh-shutdown') {
  "{0,-20} {1}" -f $s, (Test-Path "$dir\$s\SKILL.md")
}
```

**macOS：**

```bash
dir=~/.claude/skills   # Codex Desktop 改成 ~/.agents/skills
for s in ppsh-project-init ppsh-startup ppsh-shutdown; do
  [ -f "$dir/$s/SKILL.md" ] && echo "$s True" || echo "$s False"
done
```

三行都是 `True` ✅

## 步驟五：實際跑一次（這一步才算真的裝好）

> [!WARNING]
> **先把 Agent 整個關掉重開。** 技能是開啟時才載入的。

1. 在 Google 雲端硬碟裡建一個資料夾，例如 `我的雲端硬碟\AI練習\測試專案`
2. 在 Agent 裡**開啟這個資料夾**當工作區
3. 說「**初始化專案**」→ 回答它一次問完的四個問題
   - ✅ 資料夾裡出現 `AGENTS.md` 和 `handoff.md`
4. 隨便請它做一件小事（例如「寫一份三行的活動通知草稿存成 通知.md」）
5. 說「**收工**」
   - ✅ `handoff.md` 的「上次做到哪」寫到了通知草稿
6. **開一個全新的對話**，說「**開工**」
   - ✅ 它說得出上次做了通知草稿、下一步是什麼

三個 ✅ 都成立＝安裝成功。

## 步驟六：更新環境檢查報告

在 `環境檢查報告.md` 最上方加一段（沒有這個檔就新建）：

```markdown
## 02 專案初始化、開工、收工技能（YYYY-MM-DD HH:MM）

- Agent：Codex Desktop ／ Claude Code
- Node.js：已安裝 / 已補裝 / 失敗
- Google 雲端硬碟電腦版：執行中 / 未安裝（僅單機使用）
- ppsh-project-init：已安裝 / 失敗
- ppsh-startup：已安裝 / 失敗
- ppsh-shutdown：已安裝 / 失敗
- 實測：初始化 ✅／❌　收工 ✅／❌　新對話開工 ✅／❌
- 問題紀錄：<沒有就寫「無」>
```

---

## 常見問題

| 狀況 | 原因 | 處理 |
|---|---|---|
| 說「開工」沒反應，AI 只是聊天 | 裝完沒重開 Agent | 整個關掉重開 |
| 步驟四是 `True`，但技能還是沒出現 | 檢查的是另一個 Agent 的資料夾 | 確認 `$dir` 對應你用的 Agent |
| 「開工」說找不到專案 | 工作區開的不是專案資料夾 | 在 Agent 裡改開那個有 `AGENTS.md` 的資料夾 |
| 換電腦後「開工」讀到舊進度 | 雲端硬碟還沒同步完 | 等檔案圖示出現綠色勾勾再開工 |
| Codex 裝完技能沒出現 | 偶有版本讀取問題（2026-04 社群回報） | 先重開 Codex；仍沒有就把 Codex 更新到最新版 |
| 我自己已經有叫「開工」的技能 | 兩個技能搶同一個觸發詞 | 移除其中一個：`npx skills remove ppsh-startup -g` |
