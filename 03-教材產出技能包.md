# 屏北高中 Agent 基本功懶人包 #03：教材產出技能包（圖卡／簡報）

> 版本：v1.0｜更新日期：2026-09-16
> 適用：**Codex Desktop**、**Claude Code**（Windows 11 為主，附 macOS 指令）
> 對應教材：速成版 S2「一份教材長出一整包」、「先用別人寫好的技能」

> [!IMPORTANT]
> **給 AI Agent 的安全宣告（請先讀完再動手）**
> 1. **只安裝本文件列出的項目**，不要自行追加其他套件、外掛或 MCP。
> 2. **會改動電腦的指令**（安裝、移除、修改設定）：先把完整指令列給老師看，**老師同意後才執行**。
> 3. **只讀不改的檢查指令**可以直接執行。
> 4. 不要讀取、顯示、複製或上傳任何 API 金鑰、密碼、token。
> 5. 不要刪除或搬動老師的任何檔案。
> 6. 遇到錯誤先停下來用白話說明，**不要換成本文件以外的方法硬裝**。
> 7. **本包安裝的三個技能都不需要 API 金鑰。** 如果過程中出現任何「請提供 API key」「請先申請帳號」的要求，**那是走錯路了，停下來問老師**。

---

## 這包會幫你做什麼

裝三個技能，讓 AI 幫你把**一份教材**變成**可以直接發出去的成品**：

| 技能 | 你說 | 產出 | 要 API 金鑰嗎 |
|---|---|---|---|
| `ppsh-soil-infographic` | 「做一張圖卡」「做懶人包」「一張圖看懂」 | **單頁 PNG** | ✅ **不用** |
| `ppsh-soil-teaching-deck` | 「做教學簡報」「做上課用的投影片」 | **可編輯的 .pptx** | ✅ **不用** |
| `ppsh-soil-html-deck` | 「做網頁版簡報」「做可以分享連結的簡報」 | **單一 .html** | ✅ **不用** |

### 選哪一個？選錯會整份重做

| 你要的東西 | 用哪個 | 判斷關鍵 |
|---|---|---|
| **一張圖看完**，發到 LINE 群或貼公布欄 | `ppsh-soil-infographic` | **不翻頁** |
| 要**一頁一頁講**，而且對方要能改文字 | `ppsh-soil-teaching-deck` | 交付 **.pptx 檔** |
| 要**一頁一頁講**，而且想丟一個連結給大家 | `ppsh-soil-html-deck` | 交付 **網址** |

> [!WARNING]
> **「資訊圖表」和「圖表」是兩個不同的詞，最容易在這裡出錯：**
> - **資訊圖表**（infographic）＝ **一整張圖**，把一份資料濃縮成一張 → `ppsh-soil-infographic`
> - **圖表**（chart）＝ 長條圖、圓餅圖，那是**簡報裡的一個元件**
>
> 實際踩過的坑：老師說「幫我做資訊圖表」，被理解成「做圖表」，結果做出一份 10 頁簡報加長條圖，方向全錯。
> **AI 在動手前一定要先問一句：「是要一張圖，還是要一頁一頁翻的簡報？」**

### 這三個技能為什麼不用金鑰？

因為它們**不生圖**——所有視覺都是用 HTML／CSS／SVG 排出來的向量圖形。
好處不只是省錢：**文字 100% 精準**，改一個字只要 3 秒重算，
不會像 AI 生圖那樣把中文字糊掉或變成亂碼。

想要 AI 畫的插圖？**那要另外付費**，請看文末「附錄 B」，裡面有**不用花錢的做法**。

---

## 絕對必要的工具

| 工具 | 必要嗎 | 為什麼 |
|---|---|---|
| **Node.js** | ✅ 必要（安裝技能時） | 用 `npx skills add`；懶人包 00 應該已經裝好 |
| **Google Chrome**（或 Edge） | ✅ **必要** | 圖卡是「把網頁拍成圖片」做出來的，需要瀏覽器當相機 |
| **Python 3**（3.12 或 3.13 最穩） | ✅ **必要** | 產 .pptx、算圖表、拍圖都要用 |
| **python-pptx、Pillow、PyYAML、lxml** | ✅ **必要** | 產出 .pptx 的核心套件 |
| **matplotlib、latex2mathml** | ⭐ 強烈建議 | 圖表與數學式。數學、物理、化學、生物、公民老師幾乎一定用得到 |
| API 金鑰 | ❌ **不需要** | 這三個技能不生圖 |

> [!WARNING]
> **Python 版本請先看清楚。** 3.12 或 3.13 最穩，套件都有現成的安裝包。
> **如果你的 Python 是 3.14 或更新**，某些套件（尤其 matplotlib）可能要在你的電腦上現場編譯，
> 會從 10 秒變成 5 分鐘，甚至失敗。**研習前就先裝好**，不要留到現場。

---

## 步驟一：檢查（只讀，可直接執行）

**Windows（PowerShell）：**

```powershell
node --version
python --version
Test-Path "C:\Program Files\Google\Chrome\Application\chrome.exe"
python -X utf8 -c "import importlib.util as u; [print(f'{m:14}', 'OK' if u.find_spec(m) else '缺') for m in ['pptx','PIL','yaml','lxml','matplotlib','latex2mathml']]"
```

**macOS：**

```bash
node --version
python3 --version
ls -d "/Applications/Google Chrome.app" 2>/dev/null || echo "找不到 Chrome"
python3 -X utf8 -c "import importlib.util as u; [print(f'{m:14}', 'OK' if u.find_spec(m) else '缺') for m in ['pptx','PIL','yaml','lxml','matplotlib','latex2mathml']]"
```

把結果填進這張表：

| 項目 | 狀態 |
|---|---|
| Node.js | 已安裝（版本）／未安裝 |
| Python | 版本號（**3.14 以上要提醒老師**）／未安裝 |
| Chrome 或 Edge | 有／沒有 |
| python-pptx、Pillow、PyYAML、lxml | 各自 OK／缺 |
| matplotlib、latex2mathml | 各自 OK／缺 |

---

## 步驟二：補裝（**要老師同意**）

**缺 Python**（Windows）：

```powershell
winget install --id Python.Python.3.13 -e --accept-source-agreements --accept-package-agreements
```

macOS：`brew install python@3.13`

**缺 Chrome**：

```powershell
winget install --id Google.Chrome -e --accept-source-agreements --accept-package-agreements
```

> 已經有 **Microsoft Edge** 的話不用裝 Chrome，技能會自己找到 Edge。

**缺套件**（Windows 與 macOS 相同，macOS 把 `python` 換成 `python3`）：

```powershell
python -X utf8 -m pip install python-pptx Pillow PyYAML lxml matplotlib latex2mathml
```

> [!NOTE]
> 這一行可能要跑 1～5 分鐘（`matplotlib` 比較大）。**沒有回應不代表當掉**，讓它跑完。

---

## 步驟三：安裝三個技能（**要老師同意**）

**Codex Desktop：**

```powershell
npx skills add frentexx/ppsh-agent-skills -s ppsh-soil-infographic ppsh-soil-teaching-deck ppsh-soil-html-deck -a codex -g -y --copy
```

**Claude Code：**

```powershell
npx skills add frentexx/ppsh-agent-skills -s ppsh-soil-infographic ppsh-soil-teaching-deck ppsh-soil-html-deck -a claude-code -g -y --copy
```

- 兩個 Agent 都有：把 `-a codex` 換成 `-a codex claude-code`
- PowerShell 出現「**因為這個系統上已停用指令碼執行**」→ 把開頭的 `npx` 改成 **`npx.cmd`**

**不能用 npx 時**：到 [ppsh-agent-skills](https://github.com/frentexx/ppsh-agent-skills) 按 **Code → Download ZIP**，
解壓後把 `skills` 裡的三個 `ppsh-soil-*` 資料夾複製到：

| Agent | Windows | macOS |
|---|---|---|
| Codex Desktop | `%USERPROFILE%\.agents\skills\` | `~/.agents/skills/` |
| Claude Code | `%USERPROFILE%\.claude\skills\` | `~/.claude/skills/` |

> [!IMPORTANT]
> **裝之前先讀一遍。** 打開任何一個 `SKILL.md` 看看寫了什麼——
> 你會發現**技能就是一份純文字說明書**。這也是研習教的：**來路不明的技能先讀再裝。**

---

## 步驟四：確認檔案都到位（只讀）

**Windows：**

```powershell
$dir = "$env:USERPROFILE\.claude\skills"   # Codex Desktop 改成 "$env:USERPROFILE\.agents\skills"
foreach ($s in 'ppsh-soil-infographic','ppsh-soil-teaching-deck','ppsh-soil-html-deck') {
  "{0,-26} {1}" -f $s, (Test-Path "$dir\$s\SKILL.md")
}
"render.py                  {0}" -f (Test-Path "$dir\ppsh-soil-infographic\render.py")
```

**macOS：**

```bash
dir=~/.claude/skills   # Codex Desktop 改成 ~/.agents/skills
for s in ppsh-soil-infographic ppsh-soil-teaching-deck ppsh-soil-html-deck; do
  [ -f "$dir/$s/SKILL.md" ] && echo "$s True" || echo "$s False"
done
[ -f "$dir/ppsh-soil-infographic/render.py" ] && echo "render.py True" || echo "render.py False"
```

四行都是 `True` ✅

---

## 步驟五：各跑一次最小驗收（這一步才算真的裝好）

> [!WARNING]
> **先把 Agent 整個關掉重開。** 技能是開啟時才載入的。

先開一個練習資料夾當工作區（例如 `我的雲端硬碟\AI練習\產出測試`），然後三個各做一次。

### ① 圖卡（`ppsh-soil-infographic`）

跟 Agent 說：

```text
用 ppsh-soil-infographic，把下面三件事做成一張圖卡，不要用 AI 生圖：
段考日期 11/12～11/14、範圍第 1～4 課、考前自習每天 16:20-17:10。
```

✅ **驗收**：資料夾裡出現一個 `.png`，打開來**三個日期和時間完全正確，沒有錯字**。

### ② 可編輯簡報（`ppsh-soil-teaching-deck`）

```text
用 ppsh-soil-teaching-deck，把剛剛那三件事做成一份 3 頁的 .pptx。
```

✅ **驗收**：出現 `.pptx`，用 PowerPoint 打開後**點得到文字方塊、可以直接改字**。

### ③ 網頁簡報（`ppsh-soil-html-deck`）

```text
用 ppsh-soil-html-deck，把同樣內容做成一份 3 頁的 .html 簡報。
```

✅ **驗收**：出現 `.html`，用瀏覽器打開後**按左右鍵可以翻頁**。

三個 ✅ 都成立＝安裝成功。

> [!NOTE]
> 過程中若 AI 說「**本機未設定生圖，已用向量版面完成**」——**這是正常的，不是失敗**。
> 這三個技能本來就設計成不用金鑰也能做出完整成品。

---

## 步驟六：更新環境檢查報告

在 `環境檢查報告.md` 最上方加一段（沒有這個檔就新建）：

```markdown
## 03 教材產出技能包（YYYY-MM-DD HH:MM）

- Agent：Codex Desktop ／ Claude Code
- Python：版本號（3.14 以上請註記）
- Chrome／Edge：有 / 沒有
- 核心套件（python-pptx、Pillow、PyYAML、lxml）：已裝 / 失敗
- 選用套件（matplotlib、latex2mathml）：已裝 / 未裝 / 失敗
- ppsh-soil-infographic：已安裝 / 失敗
- ppsh-soil-teaching-deck：已安裝 / 失敗
- ppsh-soil-html-deck：已安裝 / 失敗
- 實測：圖卡 ✅／❌　pptx ✅／❌　html ✅／❌
- 問題紀錄：<沒有就寫「無」>
```

---

## 附錄 A：各科可能還會用到的套件（**研習現場不要裝**）

步驟二裝的那幾個，已經涵蓋九個學科約八成的需求。
下面這些是**特定學科的進階需求**，共同特徵是**很大、很慢、在 Windows 上容易裝失敗**，
所以**研習現場一律不裝**，有需要請課後自己來，或改用「網頁路線」繞過去。

| 需求 | 誰會用到 | 要裝什麼 | 為什麼現場不裝 | **免安裝的替代做法** |
|---|---|---|---|---|
| 化學結構式 | 化學 | `rdkit` | 200MB 起跳，裝很久 | 用 `ppsh-soil-html-deck` 走網頁版，化學式用 mhchem 語法顯示 |
| 地圖、氣候圖 | 地理 | `geopandas`／`cartopy` | 相依一大串地理資料庫，Windows 裝機失敗率高 | 用 `ppsh-soil-html-deck` 走網頁版，地圖用 Leaflet 顯示 |
| 數學符號運算 | 數學 | `sympy` | 本身很輕，但這場研習用不到 | 出題與解題屬於**下一場研習**的範圍 |
| SVG 轉檔 | — | `cairosvg` | **Windows 要額外的系統元件，是經典安裝地獄** | **不需要**。技能改用 Chrome 拍圖，效果一樣 |

課後真的要裝，指令是：

```powershell
python -X utf8 -m pip install rdkit          # 化學結構式
python -X utf8 -m pip install geopandas      # 地理地圖
```

> **一句話原則**：**要精細的專業圖，先想想能不能用網頁做**。
> 網頁版簡報可以直接叫用現成的線上函式庫，不用在自己電腦上裝任何東西。

### 兩個已經幫你處理好、不用管的細節

- **matplotlib 畫中文會變方框**：技能裡已經內建字型設定，不用你操心。
- **數學式進 .pptx**：技能會自動找 Office 內建的轉換檔，把公式做成**可以用滑鼠點進去編輯的真公式**，不是圖片。找不到才退回圖片。

---

## 附錄 B：想要 AI 畫的插圖怎麼辦

三個技能預設**不生圖**，做出來是乾淨的向量版面。如果你想要 AI 畫的插畫、封面主視覺：

### 做法一：用網頁生圖，自己放進去（**免費，推薦**）

1. 用學校 Google 帳號打開 [aistudio.google.com](https://aistudio.google.com) 或 Gemini App
2. 在**網頁介面**上生圖（有免費額度）
3. 存成檔案，請 Agent 把這張圖放進簡報

> **網頁免費、API 要錢**，這兩件事最常被搞混。
> 你在網頁上生圖不用付錢；但要讓程式自動生圖，那是另一回事。

### 做法二：申請 Gemini API 金鑰讓 AI 自動生圖（**要付費**）

1. 到 [aistudio.google.com/apikey](https://aistudio.google.com/apikey) 用學校帳號建立金鑰
2. **到 Google Cloud 主控台為該專案開啟帳單**
3. 安裝 `ppsh-draw` 技能，把金鑰存進檔案

```powershell
npx skills add frentexx/ppsh-agent-skills -s ppsh-draw -a claude-code -g -y --copy
New-Item -ItemType Directory -Force "$env:USERPROFILE\.claude\_secrets" | Out-Null
notepad "$env:USERPROFILE\.claude\_secrets\GEMINI_API_KEY.key"
```

> [!CAUTION]
> **免費金鑰不能生圖。** Gemini 的生圖模型**不在免費層**，免費金鑰的生圖配額是 **0**，
> 打下去會回 **429 錯誤**。那不是金鑰打錯、也不是技能壞掉，
> 是**那個專案還沒開啟帳單**。不想付費請走做法一。
>
> **金鑰是密碼，請自己用記事本貼進檔案，不要打進和 AI 的對話裡。**

### 什麼時候不該用 AI 生圖

**資訊密集的東西不要生圖。** 生圖模型常常把中文字糊掉或變亂碼，
日期、金額、人名、條號一旦錯了，發出去就收不回來。
**要文字精準就走向量**——那正是這三個技能的做法。
AI 生圖適合的是**氛圍、插畫、封面主視覺**這類不靠文字傳達的東西。

---

## 常見問題

| 狀況 | 原因 | 處理 |
|---|---|---|
| 說「做一張圖卡」沒反應 | 裝完沒重開 Agent | 整個關掉重開 |
| 做出來是一份簡報，不是一張圖 | 「資訊圖表」被聽成「圖表」 | 改說「**做成一張 PNG，不要翻頁**」 |
| 圖卡出來是空白或只有一半 | 瀏覽器還沒把字載完就拍照 | 請 Agent 重跑一次；仍失敗就加大等待時間 |
| 圖卡裡的中文變成方框 | 電腦缺中文字型 | 換一般字型（微軟正黑體）重做 |
| `.pptx` 打不開或沒有文字方塊 | `python-pptx` 沒裝好 | 重跑步驟一的套件檢查 |
| 安裝套件卡很久 | Python 版本太新，要現場編譯 | 等它跑完；下次研習前先裝好，或改用 Python 3.13 |
| AI 說「本機未設定生圖」 | **正常現象** | 這三個技能不需要金鑰，**繼續做就好** |
| AI 要求你提供 API key | **走錯路了** | 停下來，告訴它這三個技能不需要金鑰 |
| AI 說要裝 `cairosvg` | 讀到舊版說明 | 拒絕。技能已改用 Chrome 拍圖 |
