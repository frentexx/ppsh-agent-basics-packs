# 屏北高中 Agent 基本功懶人包 #00：環境檢查與基礎工具

> 版本：v1.0｜更新日期：2026-09-14
> 適用：**Codex Desktop**、**Claude Code**（Windows 11 為主，附 macOS 指令）
> 對應教材：研習開始前的環境準備

> [!IMPORTANT]
> **給 AI Agent 的安全宣告（請先讀完再動手）**
> 1. **只安裝本文件列出的項目**，不要自行追加其他套件、外掛或 MCP。
> 2. **會改動電腦的指令**（安裝、移除、修改設定）：先把完整指令列給老師看，**老師同意後才執行**。
> 3. **只讀不改的檢查指令**（例如 `git --version`）可以直接執行。
> 4. 不要讀取、顯示、複製或上傳任何 API 金鑰、密碼、token。需要登入時，請老師自己在畫面上操作。
> 5. 不要刪除或搬動老師的任何檔案。
> 6. 遇到錯誤先停下來用白話說明，**不要換成本文件以外的方法硬裝**。

---

## 這包會幫你做什麼

1. 檢查四個基礎工具有沒有裝：**Git、GitHub CLI、Node.js、uv**
2. 缺什麼補什麼，補完再檢查一次
3. Codex Desktop 使用者：檢查五個必裝外掛程式
4. 產出一份 **`環境檢查報告.md`**，研習助教一眼就看得出你卡在哪

## 先備條件

- [ ] 已安裝 **Codex Desktop** 或 **Claude Code**，而且能登入
- [ ] 電腦有網路
- [ ] 知道自己的 Windows 帳號有沒有安裝軟體的權限（學校電腦教室常常沒有，請先問資訊組）

---

## 步驟一：確認你用的是哪個 Agent

請 Agent 先回答：「我目前是在 **Codex Desktop** 還是 **Claude Code** 裡執行？」
後面第五步會依答案走不同段落。

## 步驟二：基礎工具快篩（只讀，可直接執行）

**Windows（PowerShell）：**

```powershell
git --version
gh --version
node --version
uv --version
python --version
Test-Path "C:\Program Files\Google\Chrome\Application\chrome.exe"
```

**macOS（終端機）：** 前四行相同，後兩行改成：

```bash
python3 --version
ls -d "/Applications/Google Chrome.app" 2>/dev/null || echo "找不到 Chrome"
```

請整理成表格：

| 工具 | 狀態 | 為什麼需要 | 哪一包會用到 |
|---|---|---|---|
| Git | 已安裝／未安裝 | 版本控制、commit、push | 14 GitHub |
| GitHub CLI（`gh`） | 已安裝／未安裝 | 登入 GitHub、建立 repo、Pages | 14 GitHub |
| Node.js | 已安裝／未安裝 | **安裝技能用的 `npx`**、多數 MCP | **02 技能**、10–16 MCP |
| uv | 已安裝／未安裝 | **Python 工具**安裝器 | **01 Office 讀取** |
| **Python 3** | 版本號／未安裝 | 產 .pptx、算圖表、把網頁拍成圖 | **03 教材產出** |
| **Chrome 或 Edge** | 有／沒有 | 圖卡是「把網頁拍成圖片」做的，需要瀏覽器當相機 | **03 教材產出** |

> 指令出現「無法辨識」「command not found」＝**未安裝**。

> [!WARNING]
> **Python 版本請看清楚：3.12 或 3.13 最穩。**
> 3.14 以上有些套件要在老師的電腦上現場編譯，會從 10 秒變 5 分鐘甚至失敗。
> 在報告裡把版本號寫出來，別只寫「已安裝」。

## 步驟三：缺什麼補什麼（要老師同意）

**Windows** 使用 `winget`（Windows 11 內建）。只安裝步驟二裡「未安裝」的項目：

| 工具 | 安裝指令 |
|---|---|
| Git | `winget install --id Git.Git -e --accept-source-agreements --accept-package-agreements` |
| GitHub CLI | `winget install --id GitHub.cli -e --accept-source-agreements --accept-package-agreements` |
| Node.js（LTS） | `winget install --id OpenJS.NodeJS.LTS -e --accept-source-agreements --accept-package-agreements` |
| uv | `winget install --id astral-sh.uv -e --accept-source-agreements --accept-package-agreements` |
| Python（3.13） | `winget install --id Python.Python.3.13 -e --accept-source-agreements --accept-package-agreements` |
| Chrome | `winget install --id Google.Chrome -e --accept-source-agreements --accept-package-agreements` |

> 已經有 **Microsoft Edge** 就不用裝 Chrome，03 的技能會自己找到 Edge。

**macOS**（需先有 [Homebrew](https://brew.sh)）：

```bash
brew install git gh node uv python@3.13
brew install --cask google-chrome   # 已有 Chrome 或 Edge 就跳過
```

> [!WARNING]
> **裝完之後，一定要把 Codex Desktop 或 Claude Code 整個關掉再重新開啟。**
> 新裝的工具要重開才找得到。不重開的話，下一步會誤判成「還是沒裝」。
>
> 找不到 `winget` 指令時：到 Microsoft Store 搜尋並更新「**應用程式安裝程式**」（App Installer）。

## 步驟四：再檢查一次（只讀）

重新開啟 Agent 後，再跑一次步驟二的六行指令。
這次每一項應該都有版本號。仍然失敗的項目，在報告裡標「**未安裝（安裝失敗）**」並附上錯誤訊息的第一行。

## 步驟五：外掛程式檢查

### Codex Desktop 使用者

先看設定檔（只讀）：

```powershell
Get-Content -Encoding UTF8 -Path "$env:USERPROFILE\.codex\config.toml"
```

macOS：`cat ~/.codex/config.toml`

看到下列區段且 `enabled = true`，代表已啟用：

```toml
[plugins."github@openai-curated"]
[plugins."browser-use@openai-bundled"]
[plugins."documents@openai-primary-runtime"]
[plugins."presentations@openai-primary-runtime"]
[plugins."spreadsheets@openai-primary-runtime"]
```

缺少的，請**老師自己**到 Codex Desktop：**設定（Settings）→ 外掛程式（Plugins）** 啟用：

| 外掛程式 | 用途 | 老師使用場景 |
|---|---|---|
| GitHub | 讀 repo、協助同步 | 發布教學網頁、管理懶人包 |
| Browser Use | 開網頁、截圖、測畫面 | 檢查互動教材 |
| Documents | 建立／修改 Word | 試卷、講義 |
| Presentations | 建立／修改 PowerPoint | 研習簡報 |
| Spreadsheets | 建立／修改 Excel、CSV | 成績表、題庫 |

GitHub 外掛啟用後若還沒登入帳號，標「**待連接**」即可，登入留到 **14 GitHub** 懶人包。

> [!NOTE]
> 本段依官方設定檔格式撰寫，**尚未在屏北高中研習電腦實機測試**。介面名稱若與你看到的不同，以畫面為準。

### Claude Code 使用者

Claude Code **沒有**上面這五個外掛程式，對應做法如下，不需要在這一步處理：

| Codex 外掛 | Claude Code 的對應 |
|---|---|
| Documents／Presentations／Spreadsheets | 讀取：**01 Office 讀取工具**；製作：直接請 Agent 用 Python 產出 |
| GitHub | **14 GitHub**（GitHub CLI） |
| Browser Use | 教材尚未涵蓋，之後另出懶人包 |

報告裡這一段寫「Claude Code：不適用」。

## 步驟六：產出環境檢查報告

請 Agent 在**目前的工作資料夾**建立 `環境檢查報告.md`（已存在就在最上方加一段新的，不要覆蓋舊紀錄）：

```markdown
# 環境檢查報告

- 檢查時間：YYYY-MM-DD HH:MM
- 電腦名稱：<執行 hostname 取得>
- Agent：Codex Desktop ／ Claude Code

## 外掛程式／技能檢查完成

### 基礎工具

- Git：已安裝 / 未安裝 / 已補裝
- GitHub CLI：已安裝 / 未安裝 / 已補裝
- Node.js：已安裝 / 未安裝 / 已補裝
- uv：已安裝 / 未安裝 / 已補裝

### 必裝外掛程式（Codex Desktop；Claude Code 寫「不適用」）

- GitHub：已啟用 / 未啟用 / 待連接
- Browser Use：已啟用 / 未啟用
- Documents：已啟用 / 未啟用
- Presentations：已啟用 / 未啟用
- Spreadsheets：已啟用 / 未啟用

### 問題紀錄

- <安裝失敗的項目與錯誤訊息第一行；沒有就寫「無」>

## 下一步

1. 01-Office文件讀取工具
2. 02-專案初始化開工收工技能
```

最後在對話中把報告內容完整貼一次給老師看。

---

## 常見問題

| 狀況 | 處理 |
|---|---|
| 安裝時跳出「使用者帳戶控制」視窗 | 按「是」。沒有權限按不了，請找資訊組 |
| `winget` 說「找不到符合的套件」 | 先執行 `winget source update` 再試一次 |
| 裝完還是說找不到指令 | **關掉 Agent 重開**；還不行就重新開機 |
| 電腦教室重開機後工具不見了 | 電腦有還原卡，本懶人包目前不支援這種環境，請改用個人筆電 |
